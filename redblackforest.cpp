#include <bits/stdc++.h>
using namespace std;
#define endl '\n'
#define int long long
int N;

signed main(){
    int T;
    cin >> T;
    while(T--){
        int N, K;
        vector<int> location(N);
        for(int i = 0; i<N; i++){
            cin >> location[i];
        }
        sort(location.begin(), location.end());
        
    }
}