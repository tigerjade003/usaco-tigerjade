#include <bits/stdc++.h>
using namespace std;
int n, k;
int main(){
    cin >> n >> k;
    vector<int> nums(n, 0);
    for(int i = 0; i<n; i++){
        nums[i] = i;
    }
    for(int i = 0; i<k; i++){
        
    }
}